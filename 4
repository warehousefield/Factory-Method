// Конкретная фабрика для создания калькулятора перекрытий
public class FloorCalculatorFactory : IAreaCalculatorFactory
{
    public AreaCalculator CreateCalculator()
    {
        return new FloorAreaCalculator();
    }
}
